# Triage reference answers

ILLUSTRATIVE WORKED ANSWER. Fictional AcmeFlow case. Authored reference output, not a log of model execution.

Create separate linked briefs for SSO, price and usage because they have different scope and next actions. The tool summary omits the affirmative all-paid-plans SSO sentence. The raw added text carries it.

The strongest immediate recommendation is `validate` for the Team/Okta seller-guidance decision. A participant may justify a different reversible recommendation if it preserves uncertainty and the approval gate. `continue monitoring` for the price and usage side claims requires a revisit condition.

High confidence refers only to what the preserved synthetic page says. It does not transfer to actual behavior, all account variants or private terms. A single preserved capture leaves A/B testing and other variants untested.

All generated records start pending, draft and not approved. The worked files also retain those defaults.
